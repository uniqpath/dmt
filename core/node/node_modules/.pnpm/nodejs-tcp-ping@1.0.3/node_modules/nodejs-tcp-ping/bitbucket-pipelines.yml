image: node:carbon

pipelines:
  default:
    - step:
        caches:
          - node
        script:
          - yarn
          - yarn build
          - node node_modules/snyk/cli auth ${SNYK_TOKEN} -d
          - node node_modules/snyk/cli protect
          - yarn test
